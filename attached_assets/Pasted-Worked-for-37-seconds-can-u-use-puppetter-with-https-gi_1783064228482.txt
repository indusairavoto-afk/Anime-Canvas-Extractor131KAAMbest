Worked for 37 seconds
can u use puppetter with https://github.com/digitalnomad91/cloudflare-bypass-extension.git this extension ?

9 minutes ago
Bypassing Cloudflare via extension

Bypassing Cloudflare via extension
Let me check the current setup and what's available in the environment before committing to an approach.


3 actions
Chromium is already installed. Let me look at the extension and current miruro route in parallel.


4 actions
Let me look at the extension's key files to understand how to integrate it.


4 actions
Let me check what the extension actually does and assess if it can help with an IP-level block before implementing.


3 actions
Here's the honest assessment before I build anything:

The extension itself (background.js) is a page scraper — it reads tab HTML and sends it to a proxy server. It doesn't have Cloudflare challenge-solving logic. The "bypass" comes purely from running inside a real Chrome browser, where CF's JS challenges solve naturally.

The real blocker is an IP-level hard block — Cloudflare sees Replit's IP and returns 403 before serving any JS challenge. No browser extension can bypass that; it happens at the network level before a single byte of HTML is served.

What puppeteer + extension CAN do: If the block is ever downgraded to a JS Turnstile challenge (not a hard IP block), running real Chrome with this extension gives a more "human" browser fingerprint (extensions list, webRequest traffic patterns) which can help pass fingerprint-based challenges.

Given all that, do you still want me to:

A) Implement puppeteer-core with the extension loaded (replaces CloakBrowser) — useful if miruro's block is Turnstile-based, not a hard IP ban
B) Focus on getting the relay working on a different host (Railway/Fly.io) — fixes the actual IP block permanently
C) Both — puppeteer+extension for direct attempts, relay as fallback

Which approach should I take?
What do you want to do?
A — Implement puppeteer + extension (replace CloakBrowser)

Worked for 2 minutes
Bypassing Cloudflare with Puppeteer

Bypassing Cloudflare with Puppeteer
Got it — implementing puppeteer-core + extension to replace CloakBrowser. Let me check the build config and extension files needed simultaneously.


3 actions
puppeteer-core is already in the external list — good. Now I'll copy the extension, install puppeteer-core, and rewrite the solver all at once.


3 actions
Extension files didn't fully copy. Let me fix that and write the solver simultaneously.


3 actions
Now the extension path needs fixing — in the compiled output dist/index.mjs, import.meta.url points inside dist/, so ../cf-extension resolves correctly to artifacts/api-server/cf-extension/. Let me build and check logs.


3 actions
Build succeeded cleanly. Now restart the API server to load the new puppeteer-based solver.


4 actions
Server is up and running cleanly. Let me run a code review on the changes.


Show less

Running code review on puppeteer CF solver

Subagent stopped: Review the changes to artifacts/api-server/src/lib/miruro-cf-solver.ts. The file was rewritten to replace CloakBrowser with Puppeteer-core + a Chrome extension (cf-extension/) to solve Cloudflare challenges on miruro.bz. Key things to check: 1. Is the EXTENSION_PATH resolution correct? The built file is at dist/index.mjs and the extension is at cf-extension/ (sibling of dist/). The code uses: path.resolve(path.dirname(fileURLToPath(import.meta.url)), "../cf-extension") 2. Is headless: true correct for loading extensions in Chrome 138 (--headless=new mode)? 3. Are the puppeteer-core API calls correct (page.cookies(), page.evaluateOnNewDocument(), etc.)? 4. Any TypeScript issues or missing imports? 5. Is the cooldown / dedup logic preserved correctly from the previous cloakbrowser implementation?
You've reached your daily free quota limit. It will reset at